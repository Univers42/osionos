# Playwright/test-mode overrides (vite --mode test loads this AFTER .env).
# The e2e baseline is the DEFAULT feature surface: osio.ide stays off here even
# while the dev .env bakes it on — flag-on specs opt in via localStorage.
VITE_OSIO_IDE=
